export const CATEGORY_ORDER = ["language", "frontend", "backend", "tool"] as const;

export const CATEGORY_LABELS: Record<string, string> = {
  language: "Language",
  frontend: "Frontend",
  backend: "Backend",
  tool: "Tool",
};

export const SKILL_STAGGER_DELAY = 150;
