import type { IUser } from "@/types/user";
interface HeroSectionProps {
    user: IUser;
}
export declare function HeroSection({ user }: HeroSectionProps): import("react/jsx-runtime").JSX.Element;
export {};
